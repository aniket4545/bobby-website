package configurations

const (
	//IP is the server ip
	IP string = ""
	//PORT is the server port
	PORT string = ":10000"
	//CSSFILEPATH is the path for css files
	CSSFILEPATH string = "client/src/styling"
	//JSFILEPATH is the path for css files
	JSFILEPATH string = "client/src/functionality"
	//HTMLFILEPATH is the path for css files
	HTMLFILEPATH string = "client/src/templates"
	//PATH is for DB path
	PATH string = "server/user"
	//ASSETPATH will have all image asset path
	ASSETPATH string = "client/src/assets/images"
)
